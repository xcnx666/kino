# Kino - AI Video Generation Platform - Dependencies

# LLM / Agent
openai>=1.0.0

# HTTP 异步客户端（Agnes API）
httpx>=0.24.0

# Web 服务
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
websockets>=12.0

# 配置 & 数据模型
python-dotenv>=1.0.0
pydantic>=2.0.0

# 日志
colorlog>=6.0.0

# TTS（临时方案，Agnes TTS 上线后替换）
edge-tts>=6.1.0

# 图片处理（占位图生成）
Pillow>=10.0.0

# FFmpeg 视频合成（后续模块需要）
# ffmpeg-python>=0.2.0
